apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: velin-dev
resources:
  - ../../base
patches:
  - target:
      kind: Namespace
      name: velin
    patch: |-
      - op: replace
        path: /metadata/name
        value: velin-dev
